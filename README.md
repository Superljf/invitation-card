# 中式婚礼请柬生成器

专注中式传统婚礼的在线请柬生成器，Web 端优先、纯前端、无后端、无导出，用户通过截图保存。

## 运行

```bash
npm install
npm run dev
```

## 构建与部署

```bash
npm run build
```

GitHub Pages: 推送到 `main` 后自动部署，需在仓库 Settings → Pages 中启用 GitHub Actions 作为发布源。

## 技术栈

- React 18 + Vite + TypeScript
- TailwindCSS
- localStorage 持久化
